# Runs

One file per agent per session. **Write one whether or not you found anything** —
a clean run at a new scale or a new seed is evidence, and this folder is how
coverage is demonstrated rather than claimed.

`YYYY-MM-DD-<agent>.md`, e.g. `2026-09-04-gameplay.md`.

Copy `_template.md` to start.
